name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore .github/Perch.CrossPlatform.slnx

      - name: Build
        run: dotnet build .github/Perch.CrossPlatform.slnx --no-restore --configuration Release

      - name: Test
        run: dotnet test .github/Perch.CrossPlatform.slnx --no-build --configuration Release --verbosity normal --settings coverage.runsettings

      - name: Check coverage
        run: python scripts/check_coverage.py linux

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: coverage-linux
          path: '**/TestResults/**/coverage.cobertura.xml'

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore Perch.slnx

      - name: Build
        run: dotnet build Perch.slnx --no-restore --configuration Release

      - name: Test
        run: dotnet test Perch.slnx --no-build --configuration Release --verbosity normal --settings coverage.runsettings --filter "FullyQualifiedName!~Perch.SmokeTests"

      - name: Check coverage
        run: python scripts/check_coverage.py windows

      - name: Integration smoke test
        run: pwsh tests/integration/smoke-test.ps1

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: coverage-windows
          path: '**/TestResults/**/coverage.cobertura.xml'
