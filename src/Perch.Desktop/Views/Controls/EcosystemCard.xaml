<UserControl
    x:Class="Perch.Desktop.Views.Controls.EcosystemCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Border
        x:Name="CardBorder"
        Background="{StaticResource CardBackgroundBrush}"
        BorderBrush="{StaticResource CardBorderBrush}"
        BorderThickness="1"
        CornerRadius="8"
        Padding="16"
        Width="220"
        Cursor="Hand"
        MouseLeftButtonUp="OnCardClick">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Row 0: Logo + Name -->
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Border
                        x:Name="FallbackIcon"
                        Width="32"
                        Height="32"
                        CornerRadius="16"
                        Background="{StaticResource CardBorderBrush}">
                        <TextBlock
                            x:Name="FallbackInitial"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Foreground="{StaticResource TextSubtleBrush}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                    </Border>
                    <Border CornerRadius="16" Width="32" Height="32">
                        <Border.Clip>
                            <EllipseGeometry Center="16,16" RadiusX="16" RadiusY="16" />
                        </Border.Clip>
                        <Image
                            x:Name="LogoImage"
                            Width="32"
                            Height="32"
                            Stretch="UniformToFill"
                            RenderOptions.BitmapScalingMode="HighQuality" />
                    </Border>
                </Grid>

                <TextBlock
                    Grid.Column="1"
                    Text="{Binding DisplayName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Foreground="White"
                    VerticalAlignment="Center"
                    TextTrimming="CharacterEllipsis" />
            </Grid>

            <!-- Row 1: Description -->
            <TextBlock
                Grid.Row="1"
                Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontSize="11"
                Foreground="{StaticResource TextMutedBrush}"
                TextWrapping="Wrap"
                TextTrimming="CharacterEllipsis"
                MaxHeight="32"
                Margin="0,0,0,8"
                Visibility="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource NullToVisibilityConverter}}" />

            <!-- Row 2: Aggregate status badges -->
            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <Border
                    Background="{StaticResource SuccessSubtleBrush}"
                    CornerRadius="8"
                    Padding="6,2"
                    Margin="0,0,6,0"
                    Visibility="{Binding SyncedCount, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <TextBlock FontSize="10">
                        <Run Text="{Binding SyncedCount, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" />
                        <Run Text="&#x2713;" Foreground="{StaticResource SuccessBrush}" />
                    </TextBlock>
                </Border>
                <Border
                    Background="{StaticResource WarningSubtleBrush}"
                    CornerRadius="8"
                    Padding="6,2"
                    Margin="0,0,6,0"
                    Visibility="{Binding DriftedCount, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <TextBlock FontSize="10">
                        <Run Text="{Binding DriftedCount, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" Foreground="{StaticResource WarningBrush}" FontWeight="SemiBold" />
                        <Run Text="&#x26A0;" Foreground="{StaticResource WarningBrush}" />
                    </TextBlock>
                </Border>
                <Border
                    Background="{StaticResource InfoSubtleBrush}"
                    CornerRadius="8"
                    Padding="6,2"
                    Visibility="{Binding DetectedCount, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource CountToVisibilityConverter}}">
                    <TextBlock FontSize="10">
                        <Run Text="{Binding DetectedCount, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" Foreground="{StaticResource InfoBrush}" FontWeight="SemiBold" />
                        <Run Text="&#x25CF;" Foreground="{StaticResource InfoBrush}" />
                    </TextBlock>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
